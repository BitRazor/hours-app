node_modules/

# Personal data stays local. Keep the committed "example" individual so the
# folder structure is visible; ignore every real individual's data.
individuals/*
!individuals/example/

# Cached raw ingestion payloads (can be large / re-fetchable).
knowledge/recipes/sources/*
!knowledge/recipes/sources/.gitkeep

# OS / editor noise
.DS_Store
*.log
